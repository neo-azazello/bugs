{% extends 'templates/app.twig' %}
{% block content %} 

<div class="container">
    <div class="page-header">
        <h1 class="page-title">
            Documentation of <strong>{{projectname}}</strong> project
        </h1>
    </div> 

    <div class="row">
        <div class="col-lg-4 order-lg-1 mb-4">
            {% include 'wiki/wikimenu.twig'%}
        </div>
        <div class="col-lg-8">
            <div class="card">
                <div class="card-body">
                    <div class="text-wrap p-lg-4">
                        {% include 'wiki/welcometext.twig' %}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
  {% endblock %}